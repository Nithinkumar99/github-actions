name: Reusable workflow
on: workflow_call # This allows the workflow to be called by other workflows
jobs:
    Deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Deploy
              run: echo "Deploying..."